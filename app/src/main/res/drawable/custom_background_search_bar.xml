<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor de Fundo -->
    <solid android:color="#FFFFFF" />

    <!-- Borda Arredondada -->
    <corners android:radius="16dp" />

    <!-- Sombra (simulada com borda) -->
    <stroke
        android:width="1dp"
        android:color="#DDDDDD" />

    <!-- Padding interno (opcional, para espaçamento interno) -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>
